from fastapi import FastAPI\nfrom pydantic import BaseModel\n\napp = FastAPI(title="ai_zsarpong inference API")\n\nclass InferRequest(BaseModel):\n    text: str\n\n\n@app.get("/ping")\nasync def ping():\n    return {"status": "ok"}\n\n\n@app.post("/infer")\nasync def infer(req: InferRequest):\n    # lightweight placeholder using local infer function\n    from ..ai_zsarpong_scripts.infer import infer as local_infer\n    out = local_infer(req.text)\n    return {"output": out}\n